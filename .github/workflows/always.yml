name: Verification
on:
  push:

env:
  NODE_VERSION: "18.X"
  SSH_AUTH_SOCK: /tmp/ssh_agent.sock

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    env:
      OS: linux
      ARCH: amd64
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: ls
        run: ls -la

      # SRC https://stackoverflow.com/questions/63104643/git-permission-denied-publickey-when-accessing-server-through-github-actions-c
      # - name: Setup SSH Keys and known_hosts
      #   run: |
      #       mkdir -p ~/.ssh
      #       ssh-keyscan github.com >> ~/.ssh/known_hosts
      #       ssh-agent -a $SSH_AUTH_SOCK > /dev/null
      #       ssh-add - <<< "${{ secrets.SSH_PRIVATE_KEY }}"

      # SRC https://github.com/marketplace/actions/webfactory-ssh-agent
      # - uses: webfactory/ssh-agent@v0.7.0
      #   with:
      #     ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # # XXX
      # - name: ssh git@GitHub.com
      #   run: ssh -vT git@github.com

      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: setup
        run: npm run setup:ci --if-present
      - name: npm install
        run: npm ci

      - name: lint
        run: npm run lint
